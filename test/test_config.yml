# Copyright 2026 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# =============================================================================
# Image Build Manager — Test Configuration
# =============================================================================
# Non-sensitive settings for FVT (Functional Verification Testing).
#
# Usage:
#   - Set oim_server_ip to the target server where image_build_manager runs.
#   - Leave oim_server_ip empty to run tests locally on the same machine.
#   - Configure sync options to push input files and local_repo output
#     to the target server before running tests.
#   - Sensitive credentials (SSH password) go in test_creds.yml
#     (auto-encrypted with Ansible Vault on first run).
# =============================================================================

---

# =============================================================================
# TARGET SERVER CONNECTION
# =============================================================================
# IP address of the server where image_build_manager playbook runs.
# Leave empty for local mode (running directly on the target server).
# Set to a remote IP to run all verification commands over SSH.
#
# When a remote IP is set, the framework will:
#   1. Clone the repo on that server (clone_url -> clone_path)
#   2. Sync image_build input files to the cloned input path
#   3. Optionally sync local_repo output to the target
#   4. Run all verification tests via SSH/Testinfra
oim_server_ip: ""

# SSH username for remote server (only needed for remote mode).
oim_ssh_user: root

# SSH port for remote server (only needed for remote mode).
oim_ssh_port: 22

# =============================================================================
# REPOSITORY CLONE SETTINGS (remote execution)
# =============================================================================
# Optional: Only needed if the repo is not already present on the target.
# If the repo is already cloned on the target, leave clone_url empty and
# set clone_path to the existing clone location.
#
# clone_url:    Git URL to clone on the remote target (leave empty to skip).
# clone_path:   Absolute path where the repo is/will be cloned on target.
#               The playbook runs from <clone_path>/src/.
# force_clone:  When true, deletes the existing clone and re-clones fresh.
#               When false, reuses the existing clone and runs git pull.
clone_url: ""
clone_path: "/root/image-build-manager"
force_clone: false

# =============================================================================
# DATASET CONFIGURATION
# =============================================================================
# The active dataset under test/datasets/<dataset>/.
# Each dataset folder contains:
#   config.yml                        — Top-level build configuration
#   input/image_build_config.yml      — Image build domain input file
#
# The dataset name maps to a project_name used in input/output paths.
dataset: "project_default"

# Project name used for input/output directory naming on the target server.
# Maps to: <clone_path>/src/input/<project_name>/
# Maps to: <shared_path>/output/<project_name>/
project_name: "project_default"

# =============================================================================
# SYNC — IMAGE BUILD INPUT
# =============================================================================
# When true, pushes the local dataset input files to the target server.
# Syncs: test/datasets/<dataset>/input/ -> <clone_path>/src/input/<project_name>/
#
# This ensures the target server has the correct image_build_config.yml
# before running the playbook.
sync_image_build_input: true

# =============================================================================
# SHARED PATH (runtime output on target host)
# =============================================================================
# The shared path where image_build_manager stores runtime output.
# The playbook writes build_status.yml and logs under this directory.
# Convention: /opt/omnia/image_build_manager
shared_path: "/opt/omnia/image_build_manager"

# =============================================================================
# REPORT CONFIGURATION
# =============================================================================
# Directory where test reports (JSON + HTML) are saved.
# Can be:
#   - A folder name (relative to test/): reports
#   - An absolute path: /var/reports/image_build
# No spaces allowed in the path.
report_path: reports

# Base name for report files (without extension).
# Generates: image_build_test_report.json, image_build_test_report.html
# No spaces allowed. Only letters, numbers, underscores, hyphens.
report_name: image_build_test_report

# Custom report ID (optional).
# When set, reports use this ID instead of a timestamp (YYYYMMDDHHmmSS).
# This groups multiple scenario runs under the same report entry.
# Leave empty to auto-generate from timestamp.
# No spaces allowed. Only letters, numbers, underscores, hyphens.
report_id: ""

